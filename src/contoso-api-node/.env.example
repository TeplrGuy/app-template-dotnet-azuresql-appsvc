# Environment Configuration for Contoso API Node.js

# Server Configuration
PORT=3000
NODE_ENV=development

# Azure SQL Database Configuration
# For local development, you'll need access to an Azure SQL database
# Or use a local SQL Server with compatible schema
DB_SERVER=your-server.database.windows.net
DB_DATABASE=ContosoUniversity
DB_USER=your-username
DB_PASSWORD=your-password
DB_PORT=1433
DB_ENCRYPT=true

# GitHub Copilot SDK Configuration
# Get your API key from: https://github.com/settings/tokens
# Or use GitHub Copilot chat endpoint
COPILOT_API_KEY=your-copilot-api-key-here
# Optional: Custom model to use
COPILOT_MODEL=gpt-4o-mini

# CORS Configuration
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Logging
LOG_LEVEL=info
